pipeline{

    agent any 

    stages{

       
       stage('Terraform init'){
        steps{
            sh "rm -rf .terraform"
            sh "terraform init"
        }
       }
        // plan
       stage('plan'){
        steps{
            sh "terraform plan -out=plan.out'"
        }

       }



    }
}